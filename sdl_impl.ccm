module;

#include <SDL3/SDL.h>

export module sdl:sdl_impl;

export namespace sdl {

// @bpl: pub type sdl::Event
using Event = SDL_Event;

// @bpl: pub type sdl::EventType
using EventType = SDL_EventType;

// @bpl: pub sdl::eventKeyDown: sdl::EventType
const EventType eventKeyDown = SDL_EVENT_KEY_DOWN;

// @bpl: pub sdl::eventKeyUp: sdl::EventType
const EventType eventKeyUp = SDL_EVENT_KEY_UP;

// @bpl: pub type sdl::Keycode
using Keycode = SDL_Keycode;

// @bpl: pub sdl::keyDown: sdl::Keycode
const Keycode keyDown = SDLK_DOWN;

// @bpl: pub sdl::keyUp: sdl::Keycode
const Keycode keyUp = SDLK_UP;

// @bpl: pub type sdl::KeyboardEvent = struct{key sdl::Keycode, down bool, repeat bool}
using KeyboardEvent = SDL_KeyboardEvent;

// @bpl: pub sdl::keyboardEventType: sdl::KeyboardEvent -> sdl::EventType
SDL_EventType keyboardEventType(const SDL_KeyboardEvent& event) {
  return event.type;
}

// @bpl: pub type sdl::Point = struct{x i64, y i64}
using Point = SDL_Point;

// @bpl: pub type sdl::FPoint = struct{x f32, y f32}
using FPoint = SDL_FPoint;

// @bpl: pub type sdl::FRect = struct{x f32, y f32, w f32, h f32}
using FRect = SDL_FRect;

// @bpl: pub sdl::GetRectAndLineIntersectionFloat: (sdl::FRect, sdl::FPoint, sdl::FPoint) -> bool
bool GetRectAndLineIntersectionFloat(const SDL_FRect& rect, SDL_FPoint& a, SDL_FPoint& b) {
  return SDL_GetRectAndLineIntersectionFloat(&rect, &a.x, &a.y, &b.x, &b.y);
}

// @bpl: pub sdl::PointInRectFloat: (sdl::FPoint, sdl::FRect) -> bool
bool PointInRectFloat(const SDL_FPoint& point, const SDL_FRect& rect) {
  return SDL_PointInRectFloat(&point, &rect);
}

}  // namespace sdl

export {

// @bpl: pub signnum: 'a -> f32
template <typename T> float signnum(T val) {
  return (T(0) < val) - (val < T(0));
}

}  // export
